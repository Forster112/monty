#include "monty.h"

/**
 * nop - opcode that does nothing
 * @stack: pointer to stack
 * @ln: line number
 * Return: nothing
 */
void nop(stack_t **stack, unsigned int ln)
{
	(void)stack;
	(void)ln;
}
